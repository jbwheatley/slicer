version = "3.8.4"
runner.dialect = scala3
maxColumn = 120

# Keep classic syntax — never rewrite to significant-indentation / new control syntax.
rewrite.scala3.convertToNewSyntax = false
rewrite.scala3.removeOptionalBraces = false
